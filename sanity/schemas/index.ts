import project from "./project-schema";
import page from "./page-schema";
import stories from "./story-section-schema";
import story from "./story-schema";

const schemas = [project, page, stories, story];

export default schemas;
